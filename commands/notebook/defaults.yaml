command: bash
display: quote
expand:
    type: line
commands:
    bash_session:
        command: build session client execute $_filename$
        expand:
            name: bash
            type: none
